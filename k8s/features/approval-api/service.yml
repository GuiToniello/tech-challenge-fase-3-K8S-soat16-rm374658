apiVersion: v1
kind: Service
metadata:
  name: approval-api
  namespace: oficina
  labels:
    app.kubernetes.io/name: approval-api
    app.kubernetes.io/part-of: techchallenge-oficina
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: approval-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
